server:
  port: 8086
spring:
  application:
    name: chat-websocket-gateway
chat:
  grpc:
    host: ${CHAT_GRPC_HOST:localhost}
    port: ${CHAT_GRPC_PORT:9090}
  security:
    jwt-secret: ${JWT_SECRET:demo-secret-key-change-me-123456789}
logging:
  level:
    com.demo.chatgateway: INFO
